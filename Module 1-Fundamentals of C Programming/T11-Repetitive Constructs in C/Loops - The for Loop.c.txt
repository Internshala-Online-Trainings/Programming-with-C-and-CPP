Below is the code which is used in the video.

Program 1
#include <stdio.h>
int main()
{
    int a;
    for ( a = 1; a <= 10; a++ )
    {
        printf( "%d\n", a );
    }
    getchar();
}